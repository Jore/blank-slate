{%- comment -%}

  Required arguments:

  Optional arguments:
  - account_icon
  - cart_content
  - classes
  - attrs

{%- endcomment -%}

{%- capture account_content -%}
  {%- if customer and account_logout_text -%}
    {{ account_logout_text }}
  {%- endif -%}

  {%- unless customer and account_login_text == blank -%}
    {{ account_login_text }}
  {%- endunless -%}
{%- endcapture -%}

{%- include 'button',
  icon: account_icon,
  content: account_content,
  classes: 'global-header-account',
  attrs: 'data-toggle-account' -%}

{%- assign account_content = nil -%}
