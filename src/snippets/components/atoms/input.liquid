{%- assign COMPONENT_NAME = 'input' -%}

{%- comment -%}

  Required arguments:
  - type
  - name

  Optional arguments:
  - value
  - id

{%- endcomment -%}


<input
  class="xx-input xx-input--{{- type }} {{ classes -}}"
  type="{{- type -}}"
  name="{{- name -}}"
  value="{{- value -}}"
  {%- if id != blank -%}id="{{- id -}}"{%- endif -%}
  {{ attrs }} />

{%- assign COMPONENT_NAME = nil -%}
