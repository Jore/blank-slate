{%- assign COMPONENT_NAME = 'video' -%}

{%- comment -%}

  Required arguments:
  • src

  Optional arguments:
  • poster

  - include attributes like `controls` and `loop` as attrs

{%- endcomment -%}


<video
  class="{%- include 'classes' -%}"
  src="{{- src -}}"
  {%- if poster != blank -%}poster="{{- poster -}}"{%- endif -%}
  {% include 'attrs' -%}>

  Sorry, your browser doesn't support embedded videos.

</video>

{%- assign COMPONENT_NAME = nil -%}
