{%- assign WRAPPER_TYPE = type -%}
{%- assign WRAPPER_NAME = name -%}

{% comment %}

  Required arguments:
  • type
  • content

  Optional arguments:
  • name
  • tag

{% endcomment %}


<{%- if tag != blank -%}{{- tag -}}{%- else -%}div{%- endif %}
  class="{%- include 'classes' -%}"
  {% include 'attrs' -%}>

  {{- content -}}

</{%- if tag != blank -%}{{- tag -}}{%- else -%}div{%- endif -%}>


{%- assign WRAPPER_TYPE = nil -%}
{%- assign WRAPPER_NAME = nil -%}
