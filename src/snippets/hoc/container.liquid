{%- assign CONTAINER_TYPE = type -%}
{%- assign CONTAINER_NAME = name -%}

{% comment %}

  Required arguments:
  • type
  • content

  Optional arguments:
  • name
  • tag

{% endcomment %}


<{%- if tag != blank -%}{{- tag -}}{%- else -%}section{%- endif %}
  class="{%- include 'classes' -%}"
  {% include 'attrs' -%}>

  {%- if type == 'product' and _product != blank -%}
    {%- include 'product-json' -%}
  {%- endif -%}

  {{- content -}}

</{%- if tag != blank -%}{{- tag -}}{%- else -%}section{%- endif -%}>


{%- assign CONTAINER_TYPE = nil -%}
{%- assign CONTAINER_NAME = nil -%}
